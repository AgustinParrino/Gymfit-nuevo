services:
  mysql:
    image: mysql:8.4
    container_name: gymfit-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: gymfit
      MYSQL_ROOT_PASSWORD: gymfit_local
    ports:
      - "3307:3306"
    volumes:
      - gymfit_mysql:/var/lib/mysql

volumes:
  gymfit_mysql:
